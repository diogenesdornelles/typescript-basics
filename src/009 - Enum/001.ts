//numerar opções. Cria um rol de chaves e valores

enum Cores {
  RED = 1,
  BLUE = 2,
  GREEN = 3,
}

enum Cores {
  YELLOW = 4,
}

console.log(Cores);
console.log(Cores.RED);
console.log(Cores['BLUE']);
console.log(Cores[3]);
